# 🖼️ Оптимизация изображений

Комплексная система оптимизации изображений для максимальной производительности и SEO.

## 🚀 Возможности

### 1. Автоматическая оптимизация при загрузке
- ✅ Сжатие изображений с сохранением качества (85% по умолчанию)
- ✅ Автоматическая конвертация в WebP
- ✅ Генерация миниатюр для быстрого превью
- ✅ Ресайз больших изображений (макс. 1920x1920px)
- ✅ Валидация формата и размера

### 2. Производительность
- ⚡ Lazy loading с Intersection Observer
- ⚡ Blur placeholder для плавной загрузки
- ⚡ Агрессивное кэширование (1 год)
- ⚡ Responsive images с srcset
- ⚡ Прогрессивная загрузка JPEG

### 3. SEO оптимизация
- 🔍 Правильные alt теги
- 🔍 Structured data для изображений
- 🔍 Оптимальные размеры для поисковиков
- 🔍 WebP с fallback на JPEG/PNG

## 📦 Установка

Библиотека `sharp` уже добавлена в `package.json`. Установите зависимости:

```bash
npm install
```

## 🎯 Использование

### 1. Компонент ImageUpload (для админки)

```tsx
import ImageUpload from '@/components/ImageUpload';

function MyForm() {
  const [imageUrl, setImageUrl] = useState('');

  return (
    <ImageUpload
      value={imageUrl}
      onChange={setImageUrl}
      folder="menu" // папка в /public/uploads/
      label="Product Image"
    />
  );
}
```

**Возможности:**
- Drag & drop загрузка
- Превью с hover эффектами
- Прогресс бар с процентами
- Статистика оптимизации (размер, сжатие, формат)
- Два режима: загрузка файла или URL

### 2. Компонент OptimizedImage (для фронтенда)

```tsx
import OptimizedImage from '@/components/OptimizedImage';

// С фиксированными размерами
<OptimizedImage
  src="/uploads/menu/product.jpg"
  alt="Свежевыжатый апельсиновый сок"
  width={800}
  height={600}
  priority={false} // true для изображений above-the-fold
  quality={85}
/>

// Responsive (fill)
<div className="relative w-full h-64">
  <OptimizedImage
    src="/uploads/menu/product.jpg"
    alt="Свежевыжатый апельсиновый сок"
    fill
    objectFit="cover"
    sizes="(max-width: 768px) 100vw, 50vw"
  />
</div>
```

### 3. Фоновые изображения

```tsx
import { OptimizedBackgroundImage } from '@/components/OptimizedImage';

<OptimizedBackgroundImage
  src="/uploads/hero.jpg"
  alt="Hero background"
  overlay={true}
  overlayOpacity={0.5}
  className="h-screen"
>
  <h1>Контент поверх фона</h1>
</OptimizedBackgroundImage>
```

### 4. API для загрузки

```typescript
// POST /api/upload
const formData = new FormData();
formData.append('file', file);
formData.append('folder', 'menu');
formData.append('quality', '85'); // опционально
formData.append('generateWebP', 'true'); // опционально

const response = await fetch('/api/upload', {
  method: 'POST',
  body: formData,
});

const data = await response.json();
// {
//   success: true,
//   url: '/uploads/menu/1234567890-abc123.jpg',
//   webpUrl: '/uploads/menu/1234567890-abc123.webp',
//   thumbnailUrl: '/uploads/menu/1234567890-abc123-thumb.webp',
//   placeholder: 'data:image/webp;base64,...',
//   width: 1920,
//   height: 1080,
//   originalSize: 2500000,
//   optimizedSize: 450000,
//   compressionRatio: '82.0%',
//   format: 'jpeg'
// }
```

## 🛠️ Утилиты для программной работы

### optimizeImage

```typescript
import { optimizeImage } from '@/lib/image-optimizer';

const result = await optimizeImage(
  buffer,
  'product.jpg',
  'menu',
  {
    quality: 85,
    maxWidth: 1920,
    maxHeight: 1920,
    generateWebP: true,
    generateThumbnail: true,
    thumbnailSize: 400,
  }
);
```

### validateImage

```typescript
import { validateImage } from '@/lib/image-optimizer';

const validation = await validateImage(buffer);
if (!validation.valid) {
  console.error(validation.error);
}
```

### generatePlaceholder

```typescript
import { generatePlaceholder } from '@/lib/image-optimizer';

const placeholder = await generatePlaceholder(buffer);
// Возвращает data URL для blur placeholder
```

### generateResponsiveImages

```typescript
import { generateResponsiveImages } from '@/lib/image-optimizer';

const srcset = await generateResponsiveImages(
  buffer,
  'product.jpg',
  'menu',
  [640, 750, 828, 1080, 1200, 1920]
);
// Генерирует изображения разных размеров для srcset
```

## 📊 Статистика оптимизации

После загрузки изображения в админке отображается статистика:
- ✅ Оригинальный размер
- ✅ Оптимизированный размер
- ✅ Процент сжатия
- ✅ Размеры (ширина x высота)
- ✅ Формат файла

## 🎨 Форматы изображений

### Поддерживаемые форматы
- JPEG/JPG - оптимизируется с mozjpeg
- PNG - оптимизируется с palette compression
- WebP - современный формат с лучшим сжатием
- GIF - поддерживается (но рекомендуется WebP)
- AVIF - поддерживается через Next.js Image

### Автоматическая конвертация
- Все изображения автоматически конвертируются в WebP
- Оригинальный формат сохраняется для совместимости
- Браузер автоматически выбирает лучший формат

## 🚀 Производительность

### Кэширование
```
/uploads/* - Cache-Control: public, max-age=31536000, stale-while-revalidate=86400
/images/*  - Cache-Control: public, max-age=31536000, immutable
```

### Lazy Loading
- Изображения загружаются за 50px до появления в viewport
- Используется Intersection Observer API
- Blur placeholder показывается до загрузки

### Оптимизация размера
- Автоматический ресайз больших изображений
- Сжатие с сохранением качества
- WebP экономит ~30% размера по сравнению с JPEG

## 🔧 Настройка

### next.config.js

```javascript
images: {
  formats: ['image/avif', 'image/webp'],
  deviceSizes: [640, 750, 828, 1080, 1200, 1920, 2048],
  imageSizes: [16, 32, 48, 64, 96, 128, 256, 384],
  minimumCacheTTL: 31536000, // 1 год
}
```

### Переменные окружения

Нет необходимости в дополнительных переменных. Все работает из коробки.

## 📱 Responsive Images

Автоматически генерируются версии для разных размеров экрана:
- 640w - мобильные
- 750w - iPhone
- 828w - большие мобильные
- 1080w - планшеты
- 1200w - маленькие десктопы
- 1920w - большие экраны

## 🐳 Docker

Изображения сохраняются в volume для персистентности:

```yaml
volumes:
  - ./public/uploads:/app/public/uploads
```

Это гарантирует, что загруженные изображения не потеряются при перезапуске контейнера.

## ⚠️ Ограничения

- Максимальный размер файла: 10 MB
- Минимальные размеры: 100x100 px
- Максимальные размеры: 10000x10000 px
- Поддерживаемые форматы: JPEG, PNG, WebP, GIF, AVIF

## 🎯 Best Practices

1. **Используйте priority={true}** для изображений above-the-fold
2. **Указывайте sizes** для responsive изображений
3. **Всегда добавляйте alt теги** для SEO
4. **Используйте WebP** где возможно
5. **Загружайте изображения правильного размера** - не больше чем нужно

## 🔍 SEO чеклист

- ✅ Alt теги на всех изображениях
- ✅ Оптимальные размеры файлов
- ✅ WebP с fallback
- ✅ Lazy loading для изображений ниже fold
- ✅ Правильные размеры (width/height) для CLS
- ✅ Кэширование для быстрой загрузки
- ✅ Responsive images с srcset

## 🆘 Troubleshooting

### Изображение не загружается
1. Проверьте права доступа к папке `/public/uploads`
2. Убедитесь что sharp установлен: `npm list sharp`
3. Проверьте логи: `docker logs juice-website`

### Медленная загрузка
1. Убедитесь что кэширование настроено правильно
2. Проверьте размер изображений (должны быть < 500KB)
3. Используйте WebP формат

### Ошибка оптимизации
1. Проверьте формат файла (должен быть JPEG/PNG/WebP)
2. Убедитесь что файл не поврежден
3. Проверьте размер файла (< 10MB)

## 📈 Метрики производительности

Ожидаемые улучшения:
- 📉 Размер изображений: -70-85%
- ⚡ Время загрузки: -60-80%
- 🎯 LCP (Largest Contentful Paint): < 2.5s
- 📊 CLS (Cumulative Layout Shift): < 0.1

## 🔄 Миграция существующих изображений

Если у вас есть старые изображения, вы можете их оптимизировать:

```bash
# Создайте скрипт для массовой оптимизации
node scripts/optimize-existing-images.js
```

Скрипт автоматически:
1. Найдет все изображения в `/public/uploads`
2. Оптимизирует их с помощью sharp
3. Создаст WebP версии
4. Сохранит оригиналы в backup папку

