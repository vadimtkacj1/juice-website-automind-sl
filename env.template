# Environment Variables Template
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control

# Application Environment
NODE_ENV=production
PORT=3000

# Rapyd Payment Gateway (Legacy - not used)
# Get these from: https://dashboard.rapyd.net/
RAPYD_ACCESS_KEY=your_rapyd_access_key_here
RAPYD_SECRET_KEY=your_rapyd_secret_key_here

# PayPlus Payment Gateway - Secure Payment Processing
# Get these credentials from your PayPlus merchant account at: https://www.payplus.co.il/
# 1. Sign up for PayPlus merchant account
# 2. Access your PayPlus dashboard
# 3. Navigate to API Settings
# 4. Generate API Key (UUID format, 36 characters)
# 5. Generate Secret Key
# 6. Create a Payment Page and get the Page UID
PAYPLUS_API_KEY=your_payplus_api_key_here
PAYPLUS_SECRET_KEY=your_payplus_secret_key_here
PAYPLUS_PAGE_UID=your_payplus_page_uid_here

# Optional: PayPlus API URL (defaults to production if not set)
# Development/Sandbox URL:
# PAYPLUS_API_URL=https://restapidev.payplus.co.il/api/v1.0/PaymentPages/generateLink
# Production URL (default):
# PAYPLUS_API_URL=https://restapi.payplus.co.il/api/v1.0/PaymentPages/generateLink

# ⚠️ WARNING: Test Mode - ONLY for development/testing!
# When enabled, bypasses actual payment processing - orders auto-approved without payment
# NEVER enable this in production!
# PAYPLUS_TEST_MODE=false

# Telegram Bot Configuration
# Get bot token from: https://t.me/BotFather
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_SERVICE_URL=http://localhost:3001
TELEGRAM_SERVICE_PORT=3001

# MySQL Database Configuration
# Database connection settings
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_DATABASE=juice_website

# MySQL Root Password (for Docker)
MYSQL_ROOT_PASSWORD=rootpassword

# Session Configuration
SESSION_SECRET=your_random_session_secret_here
# Generate with: openssl rand -base64 32

# Admin Authentication
# Admin credentials are stored in the database
# Use: npm run create-admin to create admin user

# Deployment Configuration
# DEPLOYMENT_URL is required for PayPlus payment callbacks
# Must be publicly accessible HTTPS URL
# Example: https://yourdomain.com (no trailing slash)
DEPLOYMENT_URL=https://your-domain.com
DEPLOYMENT_ENV=production

# Email Configuration (Optional - for order notifications)
# SMTP server configuration for sending order notifications
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-email@example.com
EMAIL_SERVER_PASSWORD=your-email-password
EMAIL_SERVER_SECURE=false

# Email settings
EMAIL_FROM_NAME=נטורליי מרענן
EMAIL_ADMIN=admin@yourdomain.com

# Examples for different providers:
# Gmail: EMAIL_SERVER_HOST=smtp.gmail.com (use App Password)
# Outlook: EMAIL_SERVER_HOST=smtp-mail.outlook.com
# Yahoo: EMAIL_SERVER_HOST=smtp.mail.yahoo.com
# PrivateEmail: EMAIL_SERVER_HOST=mail.privateemail.com

# Optional: Image Upload Configuration
# Max file size in bytes (default: 5MB)
MAX_FILE_SIZE=5242880
UPLOAD_DIR=./public/uploads


# Optional: Analytics (if needed)
# ANALYTICS_ID=your_analytics_id

# Optional: CDN URL (if using CDN for assets)
# CDN_URL=https://cdn.your-domain.com
