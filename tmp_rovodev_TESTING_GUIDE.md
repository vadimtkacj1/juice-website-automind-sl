# 📱 Руководство по тестированию исправления случайных кликов

## Что было исправлено?

Проблема: при легком касании во время прокрутки на мобильных устройствах случайно срабатывали клики по элементам (карточки меню, backdrop модального окна).

## Внесенные изменения

### 1. ✅ Создан хук `useTouchHandler` (`src/hooks/useTouchHandler.ts`)
- Определяет намерение пользователя: клик vs скролл
- Использует threshold 10px для определения движения
- Проверяет время касания (< 500ms для клика)

### 2. ✅ Исправлен `MenuItemCard.tsx`
- Удалены проблемные `onTouchEnd` с `preventDefault()`
- Добавлен `useTouchHandler` для карточек товаров
- Кнопка "+" теперь работает без случайных срабатываний

### 3. ✅ Исправлен `ProductModal.tsx`
- Backdrop теперь использует `useTouchHandler`
- Modal container блокирует всплытие touch events
- Добавлены `onTouchStart`, `onTouchMove`, `onTouchEnd` с `stopPropagation`

### 4. ✅ Улучшены CSS стили
- `touch-action: manipulation` для интерактивных элементов
- `touch-action: none` для backdrop
- `touch-action: pan-y pinch-zoom` для прокручиваемого контента
- `-webkit-tap-highlight-color: transparent` убирает синюю вспышку на iOS

## Как тестировать

### Вариант 1: Тестовая страница (быстро)
1. Откройте `tmp_rovodev_test_touch.html` в браузере на мобильном устройстве
2. Попробуйте прокручивать карточку вверх/вниз - не должно быть реакции
3. Попробуйте кликнуть - должен быть отклик
4. Смотрите статистику: "Диюк" должен быть высоким (90%+)

### Вариант 2: Реальное приложение
```bash
npm run dev
```

#### Тест 1: Карточки меню
1. Откройте страницу меню на мобильном
2. Начните прокручивать список товаров
3. Во время прокрутки случайно касайтесь карточек
4. ✅ **Ожидаемо:** Модальное окно НЕ должно открываться при скролле
5. Остановитесь и кликните на карточку
6. ✅ **Ожидаемо:** Модальное окно ДОЛЖНО открыться

#### Тест 2: Backdrop модального окна
1. Откройте модальное окно товара
2. Попробуйте прокручивать контент внутри модалки
3. Случайно касайтесь области за пределами модалки (backdrop)
4. ✅ **Ожидаемо:** Модалка НЕ должна закрываться при случайных касаниях во время скролла
5. Кликните намеренно на backdrop
6. ✅ **Ожидаемо:** Модалка ДОЛЖНА закрыться

#### Тест 3: Кнопка "+" на карточке
1. Прокручивайте список меню
2. Во время прокрутки касайтесь кнопок "+"
3. ✅ **Ожидаемо:** Модалка НЕ должна открываться при скролле
4. Остановитесь и кликните на кнопку "+"
5. ✅ **Ожидаемо:** Модалка ДОЛЖНА открыться

## Технические детали

### Алгоритм определения клика vs скролла:
```typescript
const isTap = 
  !touchMoved &&              // TouchMove не был вызван
  deltaX < 10px &&            // Движение по X меньше 10px
  deltaY < 10px &&            // Движение по Y меньше 10px
  deltaTime < 500ms;          // Длительность касания меньше 500ms
```

### Преимущества решения:
- ✅ Нативное ощущение прокрутки сохранено
- ✅ Клики работают мгновенно (нет задержек)
- ✅ Универсальный хук можно использовать везде
- ✅ CSS оптимизации улучшают производительность
- ✅ Поддержка как iOS, так и Android

## Устройства для тестирования

Рекомендуется протестировать на:
- ✅ iPhone (Safari)
- ✅ Android (Chrome)
- ✅ iPad (Safari)
- ✅ Desktop с сенсорным экраном

## Откат изменений (если нужно)

Если что-то пошло не так, откатите следующие файлы:
- `src/hooks/useTouchHandler.ts` (удалить)
- `src/app/(public)/menu/components/MenuItemCard.tsx`
- `src/components/ProductModal/ProductModal.tsx`
- `src/components/ProductModal/styles/ProductModalBase.module.css`

---

---

## 🔧 Дополнительное исправление: Скролл и Footer

### Проблема 2: Не весь контент показывается при длинном списке ингредиентов

**Причина:** Footer высотой ~130-150px, но padding-bottom был только 120px

**Решение:**
- Увеличен `padding-bottom` до **180px** на всех mobile breakpoints
- Теперь есть достаточный запас для footer + safe-area-inset

**Измененные значения:**
```css
@media (max-width: 960px) {
  .modal-content {
    padding: 20px 16px 180px; /* было 120px */
  }
}

@media (max-width: 480px) {
  .modal-content {
    padding: 16px 16px 180px; /* было 120px */
  }
}
```

**Тестовый файл:** `tmp_rovodev_test_long_ingredients.html`
- 30+ ингредиентов в разных группах
- Визуальный индикатор прокрутки
- Проверка доступности всего контента

---

**Дата исправления:** 2026-01-28  
**Затронутые компоненты:** MenuItemCard, ProductModal, Touch Handler Hook, Footer Layout
