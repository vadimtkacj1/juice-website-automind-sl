# 🚀 Инструкция по установке и запуску

## Шаг 1: Установка зависимостей

Откройте терминал в папке проекта и выполните:

```bash
npm install
```

Это установит все необходимые библиотеки, включая `sharp` для оптимизации изображений.

## Шаг 2: Проверка установки

Убедитесь что `sharp` установлен правильно:

```bash
npm list sharp
```

Должно показать что-то вроде:
```
juice-website@1.0.0 C:\Users\vadim\Downloads\Juice website
└── sharp@0.33.1
```

## Шаг 3: Запуск в режиме разработки

### Вариант 1: Локально (без Docker)

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

### Вариант 2: С Docker

```bash
docker-compose up --build
```

Сайт будет доступен по адресу: http://localhost:3000

## Шаг 4: Оптимизация существующих изображений (опционально)

Если у вас уже есть загруженные изображения в папке `public/uploads`:

```bash
npm run optimize-images
```

Это:
1. Создаст backup в `public/uploads-backup`
2. Оптимизирует все изображения
3. Создаст WebP версии
4. Создаст миниатюры

## Шаг 5: Тестирование

1. Откройте админку: http://localhost:3000/admin
2. Войдите с вашими учетными данными
3. Перейдите в "Menu" → "Add Item"
4. Попробуйте загрузить изображение
5. Вы должны увидеть:
   - Прогресс бар
   - Статистику оптимизации
   - Превью изображения

## 🎯 Что должно работать

### При загрузке изображения:
- ✅ Прогресс бар показывает процент загрузки
- ✅ Появляется зеленое сообщение с статистикой
- ✅ Изображение отображается в превью
- ✅ Hover эффект на превью

### В папке uploads должны появиться:
- `1234567890-abc123.jpg` - оптимизированное изображение
- `1234567890-abc123.webp` - WebP версия
- `1234567890-abc123-thumb.webp` - миниатюра

## 🐛 Возможные проблемы

### Ошибка: "Cannot find module 'sharp'"

**Решение:**
```bash
npm install sharp --save
```

Если не помогло (на Windows):
```bash
npm install --platform=win32 --arch=x64 sharp
```

### Ошибка: "EACCES: permission denied"

**Решение:**
```bash
# Windows (PowerShell от администратора)
icacls "public\uploads" /grant Users:F /t

# Linux/Mac
chmod -R 755 public/uploads
```

### Изображения не загружаются в Docker

**Решение:**
Убедитесь что volume настроен в `docker-compose.yml`:
```yaml
volumes:
  - ./public/uploads:/app/public/uploads
```

Пересоздайте контейнер:
```bash
docker-compose down
docker-compose up --build
```

### Ошибка: "Image optimization timed out"

**Решение:**
Изображение слишком большое. Уменьшите размер файла до < 5 MB перед загрузкой.

## 📊 Проверка производительности

### До оптимизации:
1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Загрузите страницу с изображениями
4. Посмотрите размер файлов

### После оптимизации:
Размер должен уменьшиться на 70-85%!

### Проверка кэширования:
1. Откройте DevTools → Network
2. Обновите страницу (F5)
3. Кликните на изображение
4. В Headers должно быть:
   ```
   Cache-Control: public, max-age=31536000, stale-while-revalidate=86400
   ```

## 🎉 Готово!

Теперь система оптимизации изображений работает!

### Что дальше?
1. Загрузите несколько изображений через админку
2. Проверьте что они оптимизируются
3. Посмотрите на статистику
4. Наслаждайтесь быстрым сайтом! 🚀

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте логи: `docker logs juice-website`
2. Проверьте права доступа к папке `public/uploads`
3. Убедитесь что `sharp` установлен: `npm list sharp`
4. Попробуйте переустановить зависимости: `rm -rf node_modules && npm install`

